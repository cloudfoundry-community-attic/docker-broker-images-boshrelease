---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - docker_broker_images
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: docker-broker-images
    version: latest
